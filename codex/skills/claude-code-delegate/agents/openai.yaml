interface:
  display_name: "Claude Code Delegate"
  short_description: "Send bounded prompts to visible or streamed Claude Code workers."
  default_prompt: "Send a bounded worker prompt to Claude Code, keep the work visible or streamed as needed, then inspect and verify the result."
