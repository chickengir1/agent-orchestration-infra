interface:
  display_name: "Claude Code Delegate"
  short_description: "Send prompts into a visible Claude Code Terminal worker."
  default_prompt: "Start a visible Claude Code worker, send the bounded prompt into that same session, then inspect and verify the result."
