interface:
  display_name: "Prompt Engineering Harness"
  short_description: "Compress and test prompts with eval cases."
  default_prompt: "Use $prompt-engineering-harness to compress this prompt, define its behavior contract, and build an eval matrix."
